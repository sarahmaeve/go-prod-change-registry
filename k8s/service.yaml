apiVersion: v1
kind: Service
metadata:
  name: pcr-server
  namespace: pcr
spec:
  selector:
    app: pcr-server
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
